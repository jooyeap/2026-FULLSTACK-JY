<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.the703.dao.UserInfoMapper">
	<!-- 메서드이름 , 파라미터 -->
    <insert id="insert" parameterType="com.the703.dto.UserInfoDto">
    	insert into userinfo_e (email, age) values (#{email},#{age})
    </insert>
    	
<!-- mybatis-config.xml 파일에서 typeAlias로 UserInfoDto로 별명 설정했으니 사용 -->
    <update id="update" parameterType="UserInfoDto">
    	update userinfo_e set email=#{email}, age=#{age} where no= #{no}
    </update>

    <delete id="delete" parameterType="int">
    	delete from userinfo_e where no=#{no}
    </delete>
    
    <!-- 리턴값, 메서드이름, 파라미터 -->
    <select resultType="UserInfoDto" id="selectAll">
    	select * from userinfo_e
    </select>
    
    <select resultType="UserInfoDto" id="select" parameterType="int">
    	select * from userinfo_e where no=#{no}
    </select>
    
</mapper>
<!-- 
insert : 
- insert into userinfo_e (email, age) values (?,?);
select (전체):
- select * from userinfo_e;
select (해당번호의 읽기):
- select * from userinfo_e where no= ?;
update (해당번호 수정) : 
- update userinfo_e set email=?, age=? where no= ?;
delete (해당번호 삭제) : 
- delete from userinfo_e where no=? ; 
 -->