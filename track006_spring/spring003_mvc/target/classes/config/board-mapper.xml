<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.the703.dao.BoardMapper">
  <insert id="insert" parameterType="BoardDto">
	insert into mvcboard2 (bname, bpass, btitle, bcontent, bdate, bip)
	values(#{bname}, #{bpass}, #{btitle}, #{bcontent}, now(), #{bip})
  </insert>
  
  <update id="update" parameterType="BoardDto">
  	update mvcboard2 set btitle=#{btitle}, bcontent=#{bcontent} where bno=#{bno} and bpass=#{bpass}
  </update>
  
  <update id="updateBhit" parameterType="BoardDto">
  	update mvcboard2 set bhit= bhit+1 where bno=#{bno} 
  </update>
  
  <delete id="delete" parameterType="BoardDto">
  	delete from mvcboard2 where bno=#{bno} and bpass=#{bpass}
  </delete>
  
  <select id="selectAll" resultType="BoardDto">
  	select * from mvcboard2 order by bno desc
  </select>
  
  <select id="passCheck" resultType="int" parameterType="BoardDto">
  	select count(*) from mvcboard2 where bno=#{bno} and bpass=#{bpass}
  </select>
  
  <select id="select" parameterType="int" resultType="BoardDto">
  	select * from mvcboard2 where bno=#{bno}
  </select>
  
</mapper>