📑 전자결재 문서 관리 (Approval System)
문서의 생명 주기(Lifecycle)를 관리하고 승인 라인을 추적하는 핵심 비즈니스 로직 파트입니다.

📥 Create (작성 및 제출)
결재 문서 초안 작성 / 제출

문서 제목, 내용, 결재선 정보(누가 순서대로 승인할지)를 입력하여 테이블에 저장합니다.

임시저장 상태일 때는 문서 번호만 따놓고, 상신(제출) 버튼을 누르면 상태가 대기로 바뀌며 결재 프로세스가 시작됩니다.

--> insert into approval_document (title, content, status, writer_id) values (...)

결재선 지정

해당 문서에 순서대로 결재할 사람들을 매핑 테이블에 저장합니다. (예: 1차 승인자 팀장, 2차 승인자 부장)

--> insert into approval_lines (doc_id, approver_id, sequence) values (...)

결재 이력(로그) 초기화

문서 생성 및 최초 상신 시, '기안' 단계의 첫 로그를 자동으로 기록합니다.

📤 Read (조회 및 열람 분기)
내가 올린 결재 문서 목록 조회

내가 기안한 문서들의 현재 진행 상황(대기/진행중/완료/반려)을 한눈에 확인합니다.

--> select * from approval_document where writer_id = 현재사용자ID

내가 승인해야 할 문서 목록 조회

결재선 상에서 내 차례가 되어 결재를 기다리고 있는 문서들만 필터링하여 보여줍니다.

--> select * from approval_document d join approval_lines l on d.id = l.doc_id where l.approver_id = 현재사용자ID and d.status = '대기/진행중'

결재 문서 상세 내용 및 이력 조회

특정 문서의 본문내용과 함께, 이 문서가 누구누구를 거쳐 승인되었는지 혹은 왜 반려되었는지 이력 로그를 함께 화면에 뿌려줍니다.

🔒 권한별 열람 분기 처리 (보안)

기안자, 결재선에 명시된 결재자들만 상세 내용을 볼 수 있도록 차단/허용 로직을 구현합니다. (아무나 URL 주소를 쳐서 남의 결재 문서를 보면 안 되기 때문)

✏️ Update (상태 변경 및 결재선 수정)
⚙️ 결재 상태 머신 (State Machine) 관리

문서의 상태 변화 로직을 안전하게 처리합니다.

임시저장 ➔ 상신(대기) ➔ 진행중 ➔ 최종승인 / 반려

승인(Approve) 처리

내가 승인 버튼을 누르면 문서 상태를 업데이트합니다. 내가 마지막 결재자라면 최종승인으로, 다음 결재자가 남아있다면 진행중 상태를 유지하며 다음 사람에게 알림을 보냅니다.

--> update approval_document set status = '진행중' (또는 '최종승인') where id = 문서ID

반려(Reject) 처리

중간에 한 사람이라도 반려를 누르면, 뒤에 결재자가 얼마나 남았든 상관없이 문서 상태는 즉시 반려로 종료됩니다. 이때 반려 사유를 필수적으로 입력받아 저장합니다.

--> update approval_document set status = '반려', reject_reason = '예산 부족' where id = 문서ID

❌ Delete (삭제)
문서 삭제 (조건부 삭제)

이미 결재가 진행 중이거나 최종 완료된 문서는 증빙 자료이므로 절대 삭제할 수 없도록 막아야 합니다.

오직 아직 제출하지 않은 '임시저장 상태'의 문서 또는 '반려된 문서'에 한해서만 삭제(물리적/논리적 삭제)를 허용합니다.

--> delete from approval_document where id = 문서ID and status in ('임시저장', '반려')

🪵 결재 이력 로그 (Approval History Log)
감사(Audit) 및 추적을 위해 시스템 내부적으로 자동 수행되는 내역 관리 파트입니다.

이력 자동 생성 (Create Only)

결재선 상에서 발생하는 모든 행위([기안] ➔ [승인] ➔ [반려])가 일어날 때마다 누가, 언제, 어떤 액션을 취했는지, 그리고 의견(반려 사유 등)은 무엇인지를 이력 테이블에 실시간으로 기록합니다.

이 데이터는 나중에 법적 증빙이나 감사 자료로 쓰일 수 있으므로 수정(Update)과 삭제(Delete)가 절대 불가능한 구조(Immutable)로 설계해야 합니다.

--> insert into approval_history (doc_id, user_id, action, comment, created_at) values (...)

//////////////////////////////////////////////////////////////
1.Step 1: 데이터베이스(DB) 엔티티 설계:

필수 테이블: 사원 테이블(User), 결재 문서 테이블(Document),
           결재선 테이블(ApprovalLine), 결재 이력 테이블(History)
핵심 포인트: 결재선 테이블에 seq(순서, 1, 2, 3...) 필드와 status(대기/승인/반려)

2.Step 2: 회원 인증 및 권한 관리 (Spring Security + JWT) !! 제일 먼저

회원가입, 로그인(토큰 발급)
그리고 API 요청 시 토큰을 검증하는 Security Filter까지 먼저 완성

3.Step 3: 문서 생성(Create) 및 조회(Read)

결재 문서의 기본 작성과 목록 조회부터 구현
우선순위: 임시 저장 및 기안(제출) 기능을 먼저 만듭니다.본인이 올린 문서 목록 조회, 그리고 결재선 테이블을 조인(JOIN)하여 **"내 순서(seq)이면서 상태가 대기인 문서"**를 가져오는 '결재 대기함 조회' 쿼리를 작성합니다.

4.Step 4: 상태 머신(Update) 및 이력 로그(History) 결합:가장 까다롭고 중요한 핵심 로직.마지막으로 승인/반려에 따른 상태 변화와 로그 자동화 단계입니다.
승인 로직 순서:현재 결재자의 상태를 승인으로 변경다음 seq를 가진 결재자가 있는지 확인있다면? 문서 상태는 진행중 유지 + 다음 결재자에게 알림없다면(내가 마지막)? 문서 상태를 최종승인으로 변경반려
로직: 반려 버튼 클릭 시 즉시 문서 상태를 반려로 바꾸고 반려 사유 저장이력 자동화: 위의 승인/반려/기안 처리가 일어나는 서비스 메서드 끝에 History 테이블로 insert하는 공통 메서드를 붙이거나, 스프링 이벤트를 발행하여 로그를 남깁니다.

/////////////////////////////////////////////////////////////////////////

결재 상태 머신 테이블 명

TEMP: 임시저장 (아직 제출 안 함)
STANDBY: 상신/대기 (결재선 상의 첫 번째 사람의 승인을 기다리는 중)
PROGRESS: 진행중 (중간 결재자는 승인했으나 최종 승인이 남음)
APPROVE: 최종승인 (모든 결재자가 승인 완료)
REJECT: 반려 (누군가 거절함)

/////////////////////////////////////////////////////////////////////////

erp_erd_mybatis.html 파일

내 담당 테이블?

USERS                           로그인 계정 정보
create table USERS(
    user_id int primary key,
    email varchar,
    password varchar,
    role varchar,
    is_active tinyint,
    created_at datetime
);

user_id int primary key,        유저 아이디 , 고유 키
email varchar,                  이메일
password varchar,               비밀번호
role varchar,                   권한
is_active tinyint,              참,거짓형  // tinyint 가장 작은 int형 자료형
created_at datetime             생성시기


EMPLOYEE                        사원 정보
create table EMPLOYEE(
    emp_id int primary key,
    company_id int,
    foreign key (company_id) references COMPANY(company_id),
    dept_id int,
    foregin key (dept_id) references DEPARTMENT(dept_id),
    user_id int,
    foregin key (user_id) references USERS(user_id),
    emp_name varchar,
    position varchar,
    phone varchar,
    profile_img varchar,
    joined_at date
);

emp_id int primary key,         직원 id 
company_id int,                 COMPANY FK
foreign key (company_id) references COMPANY(company_id),
dept_id int,                    DEPARTMENT FK 
foregin key (dept_id) references DEPARTMENT(dept_id),
user_id int,                    USERS FK
foregin key (user_id) references USERS(user_id),
emp_name varchar,               이름
position varchar,               직급
phone varchar,                  휴대폰번호
profile_img varchar,            프로필이미지
joined_at date                  입사일?


APPROVAL_DOC
create table APPROVAL_DOC(
    doc_id int primary key,
    company_id int,
    foreign key (company_id) references COMPANY(company_id),
    author_emp_id int,
    foreign key (author_emp_id) references EMPLOYEE(emp_id).
    title varchar,
    content text,
    status varchar,
    submitted_at datetime,
    updated_at datetime
);

APPROVAL_LINE

/////////////////////////////////////////////////////////////////////////